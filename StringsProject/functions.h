#pragma once
#include <fstream>
#include <string>
#include "structures.h"

bool menu(int& index);
int randomIndexWord();
GAME getRandomWord(int& index);
GAME getRandomWord(int& index);
void guessTheWord(int& index);

